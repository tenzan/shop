<%= simple_form_for([:admin, category, product]) do |f| %>
   <%= f.input :name %>
   <%= f.input :description %>
   <%= f.input :price %>
   <%= f.input :image %>

   <%= f.button :submit, class: "btn-primary" %>
<% end %>
