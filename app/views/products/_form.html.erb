<%= simple_form_for([category, product]) do |f| %>
   <%= f.input :name %>
   <%= f.input :description %>
   <%= f.input :price %>
   <%= f.file_field :image %>

   <%= f.button :submit, class: "btn-primary" %>
<% end %>
